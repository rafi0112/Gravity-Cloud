# config.yaml — LiteLLM model list (unused by main.py; kept for reference only)
# Hardcoded IPs have been removed. Model routing is handled by main.py via
# LOCAL_OLLAMA_SERVICE_URL and EDGE_NODE_2_URL environment variables.
model_list: []